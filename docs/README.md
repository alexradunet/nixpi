# Documentation

This directory currently contains a minimal index while the policy docs are being reworked.

## Current Focus

- NixOS VM hardening defaults in `infra/nixos/vm.nix`
- Reproducible build/check workflow via `flake.nix` and `scripts/check.sh`

## Planned Docs (to be reintroduced)

- Risk tiers
- Trust charter
- Data policy

If needed, recover previous versions from git history.
